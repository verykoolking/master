<?xml version="1.0" encoding="UTF-8" ?>
<script name="cm_pws.test.generateLogFileName">
  <group>scripts</group>
  <label>generateLogFileName</label>
  <metaVersion>4</metaVersion>
  <script><![CDATA[
    var today = Calendar.formatDate(new Date(),"ddMMMyyyy");
    var logLocation = Global.getConfigVariable("logFilePath");
    var project = "NGAA";
    if (!logLocation.endsWith("/")||!logLocation.endsWith("\\")){
        logLocation += "/";
    }
    var fileName =  logLocation + project + "-" + today + ".log";
    return fileName;
  ]]></script>
</script>