<?xml version="1.0" encoding="UTF-8" ?>
<script name="cm_spoc.rule_DDuoLengthCondRule">
  <group>rules_scripts.Lists</group>
  <label>rule_DDuoLengthCondRule</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="data" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="errList" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    /*

    Conditional

    DDuoLengthCondRule

    The attribute must be saved in the catalog only if the related attribute assumes the value shown in the column H.

    Duo
    DuoLength

    Duo
    CheckLength

    TRUE

    */
    var varCheck = data.Duo;

    if( varCheck.DuoLength && !varCheck.CheckLength)
    {
         errList.addErrorMessage(data,["Duo Length can be saved only if Check Length is set"],"NGAA_003","E");
    }
    return false;
  ]]></script>
</script>