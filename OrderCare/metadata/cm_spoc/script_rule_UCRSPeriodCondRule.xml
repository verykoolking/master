<?xml version="1.0" encoding="UTF-8" ?>
<script name="cm_spoc.rule_UCRSPeriodCondRule">
  <group>rules_scripts.BoltOnType</group>
  <label>rule_UCRSPeriodCondRule</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="data" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="errList" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    /*
     * UCRSTypeOfPeriodCondRule.Period saved only if :
        UCRelatedSpecification      .TypeOfPeriod in [DAYS, MONTHS, MINUTES]
        GeneralStartEndDateSpec      StartDateTime       is EMPTY
        GeneralStartEndDateSpec      EndDateTime       is EMPTY
     *
     */

     var period = data.UCRelatedSpecificationDS.Period;
     var top = data.UCRelatedSpecificationDS.TypeOfPeriod;
     var gdsDS = data.GeneralStartEndDateSpecDS;

     if(period) {

         if(
             (!top || top!=="DAYS" || top !=="MONTHS" || top !== "MINUTES")  &&
             (gdsDS.StartDateTime || gdsDS.EndDateTime)
            ) {
             errList.addErrorMessage(data,["Period can't be saved if Type Of Period is not set and Start and End date are set"],"NGAA_003","E");
         }
     }

     return false;
  ]]></script>
</script>