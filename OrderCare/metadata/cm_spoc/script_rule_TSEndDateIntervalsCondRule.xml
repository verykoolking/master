<?xml version="1.0" encoding="UTF-8" ?>
<script name="cm_spoc.rule_TSEndDateIntervalsCondRule">
  <group>rules_scripts.Tribes</group>
  <label>rule_TSEndDateIntervalsCondRule</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="data" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="errList" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    // The attribute (EndDate) value must be greater than the value of related attribute. Moreover, the related attribute (StartDate) should assume the value shown in the column H.

    var dt=data;

    if (dt.StartDate != null && dt.StartDate != "" && dt.EndDate < dt.StartDate){
        errList.addErrorMessage(data,["EndDate must be greater than StartDate"],"NGAA_003","E");
    }
    return false;
  ]]></script>
</script>