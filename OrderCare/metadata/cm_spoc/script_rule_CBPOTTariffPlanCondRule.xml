<?xml version="1.0" encoding="UTF-8" ?>
<script name="cm_spoc.rule_CBPOTTariffPlanCondRule">
  <group>rules_scripts.Operation</group>
  <label>rule_CBPOTTariffPlanCondRule</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="data" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="errList" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    /*if (data.TariffPlan && data.BanningByTechnologyOrByTP!="P")
    {
        errList.addErrorMessage(data,["Tariff Plan Identifier must be empty for this Banning."],"NGAA_003","E");
    }
    return;*/

    //var lg=data.ConstraintsCByPlanOrTArr.ListDS.lenght;
    //var lg=data.parent.length;
    //var entity=data.parent;//.ConstraintByPlanOrTechnolog;+

    var lg=data.ListDS.length;
    var entity=data.ListDS;//.ConstraintByPlanOrTechnolog;
    var ContOP=0;
    var ContSer=0;

    if (lg > 0){
        for (var i=0; i<lg; i++)
        {
            if (entity[i].TariffPlan!=null && entity[i].BanningByTechnologyOrByTP!="P"){
                ContOP++;
            }
         }

        if (ContOP>0){
        errList.addErrorMessage(data,["Tariff Plan must be empty when Banning isn't P."],"NGAA_003","E");
        }
    }
    return;
  ]]></script>
</script>